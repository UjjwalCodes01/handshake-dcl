# Build output — regenerated by `npm run build`
bin/

# Dependencies
node_modules/

# NOTE: package-lock.json is deliberately COMMITTED.
# The SDK is pinned to an exact auth-server prerelease build; without the lock
# file a fresh clone can resolve a different build and lose the Multiplayer
# Server APIs entirely.

.DS_Store
**/.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
